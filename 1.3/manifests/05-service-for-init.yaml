apiVersion: v1
kind: Service
metadata:
  name: nginx-init-service
  namespace: default
  labels:
    app: nginx-init-service
    homework: "1.3"
spec:
  selector:
    app: nginx-init
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
